version: '3.8'

services:
  # PostgreSQL - Base de données
  postgres:
    image: postgres:15-alpine
    container_name: rapidodevis-postgres
    environment:
      POSTGRES_DB: rapidodevis
      POSTGRES_USER: rapido
      POSTGRES_PASSWORD: SecurePass2025!
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database_schema.sql:/docker-entrypoint-initdb.d/01_schema.sql
    networks:
      - rapidodevis-network
    restart: unless-stopped

  # PgAdmin - Interface web PostgreSQL
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: rapidodevis-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@rapido-devis.fr
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    networks:
      - rapidodevis-network
    depends_on:
      - postgres

  # Metabase - Dashboards BI
  metabase:
    image: metabase/metabase:latest
    container_name: rapidodevis-metabase
    environment:
      MB_DB_TYPE: postgres
      MB_DB_DBNAME: metabase
      MB_DB_PORT: 5432
      MB_DB_USER: rapido
      MB_DB_PASS: SecurePass2025!
      MB_DB_HOST: postgres
    ports:
      - "3000:3000"
    networks:
      - rapidodevis-network
    depends_on:
      - postgres
    restart: unless-stopped

networks:
  rapidodevis-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local